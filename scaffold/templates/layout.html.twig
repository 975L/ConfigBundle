{# The app's own page shell, extended by every scaffolded template (login, reset password, the error pages). Twig takes the first of the list that exists: SiteBundle's full layout - header, footer, navigation, SEO, theme - whenever a site foundation is installed, else UiBundle's minimal shell. Nothing to change when adding or removing SiteBundle, and this file is yours to replace outright with your own markup #}
{% extends ['@c975LSite/layout.html.twig', '@c975LUi/layout.html.twig'] %}
